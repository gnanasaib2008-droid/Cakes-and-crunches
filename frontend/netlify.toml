# Netlify Deployment Configuration

[build]
  command = "npm run build"
  publish = "dist"

# SPA redirection rules to prevent 404 errors on deep route reloads
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
